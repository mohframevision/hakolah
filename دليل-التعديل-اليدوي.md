# دليل التعديل اليدوي البسيط

هذا الدليل لك أنت، صاحب الموقع، مو لمبرمج. الهدف: تقدر تسوي تعديلات **بسيطة** بنفسك من متصفحك مباشرة — بدون ما تثبّت أي برنامج بجهازك، وبدون ما تحتاج تنتظر رد مني.

**قبل لا تبدأ:** هذا الدليل لتعديلات صغيرة (نص، رقم، لون). لأي شي أكبر — ميزة جديدة، تصميم كامل، إصلاح عطل — اطلب مني كالعادة. جرّب أي تعديل هنا وأنت مرتاح: أسوأ اللي يصير إنه يظهر خطأ بالموقع، وترجعه بضغطة زر (اشرح تحت "لو خربت شي").

---

## الأداة: محرر GitHub المباشر

ما تحتاج تنزّل الكود ولا تثبّت شي. كل ملفات الموقع موجودة بموقع GitHub، وفيه فيه زر تعديل قلم ✏️ بأعلى يمين أي ملف تفتحه. تضغطه، تعدّل النص، تضغط "Commit changes" بالأسفل — وخلال دقيقة إلى دقيقتين يتحدّث الموقع الحي تلقائياً (نفس الآلية اللي تنشر تعديلات لوحة `/admin`).

**الخطوات بالتفصيل:**
1. روح لمستودع الموقع على GitHub (`github.com/mohframevision/hakolah`).
2. دوّر الملف اللي تبي تعدّله (الأمثلة تحت تقول لك المسار بالضبط، مثلاً `src/js/main.js`).
3. اضغط على اسم الملف يفتح محتواه.
4. اضغط أيقونة القلم ✏️ (أعلى يمين الصفحة).
5. عدّل بس الجزء اللي تبيه — لا تلمس شي غيره.
6. انزل لتحت، اكتب جملة قصيرة توضح وش غيّرت (مثلاً: "تغيير نص الفوتر")، واضغط الزر الأخضر **"Commit changes"**.
7. خله يحفظ مباشرة على `main` (الخيار الافتراضي المحدد).
8. انتظر دقيقة-دقيقتين، افتح الموقع الحي وحدّث الصفحة (Ctrl+Shift+R لو ما تغيّر الشي).

---

## أمثلة حقيقية آمنة (من كود موقعك فعلياً)

### ١. تغيير نص ثابت بالموقع
الملف: `src/_data/i18n.js`

كل نصوص الواجهة الثابتة (زر "الأقرب مني"، نص حقوق الفوتر، وغيرها) بهذا الملف، مرة بالعربي ومرة بالإنجليزي. مثال حقيقي موجود الآن:

```
footer_copyright: "© 2026 هكوله. جميع الحقوق محفوظة.",
```

لو تبي تغيّر السنة أو النص، غيّر بس الجزء بين علامتي التنصيص `" "` واترك الباقي (الفاصلة بالنهاية مهمة، لا تحذفها). **لكل نص عربي فيه نص إنجليزي مقابل بنفس الملف تحت** — الموقع ما يرفض النشر لو عدّلت وحده بس، لكن النسخة الإنجليزية تبقى بالنص القديم وتصير غير مطابقة للعربي، فعدّل الاثنين مع بعض دايماً حتى تبقى النسختان متوافقتين.

### ٢. تغيير رقم بسيط
الملف: `src/js/main.js`

مثال حقيقي: عدد رقائق التصنيف اللي تظهر قبل ما يحتاج الزائر يضغط "عرض المزيد" مضبوط بسطر واحد:

```
const FILTER_CHIP_LIMIT = 10;
```

غيّر الرقم `10` لأي رقم ثانٍ (مثلاً `15`) واحفظ. أي رقم من هذا النوع (مكتوب صريح جنب اسم بالإنجليزي بأول السطر) عادة آمن التعديل.

### ٣. تغيير لون الموقع يدوياً (احتياطي)
الطريقة العادية صارت من لوحة `/admin` مجموعة "شكل الموقع" (أسهل، فيها منتقي لون بصري). لكن لو حبيت تعدّل مباشرة، الملف هو `src/_data/theme.json`:

```json
{
  "primary": "#1b4d3e",
  "primaryDarkMode": "#3fa480"
}
```

غيّر قيمة `primary` (الوضع الفاتح) و`primaryDarkMode` (الوضع الداكن) لأي كود لون تبيه (تقدر تجيب كود اللون من أي موقع بحث "color picker"). لازم يبدأ بـ`#` ويتبعه ٦ حروف/أرقام بالضبط.

---

## آمن تلمسه ✅ مقابل لا تلمسه أبداً 🚫

**آمن (نصوص، أرقام بسيطة):**
- أي قيمة بين علامتي تنصيص `" "` بملف `i18n.js`
- أي رقم واضح بسطر `const اسم_بالإنجليزي = رقم;`
- ألوان بملف `theme.json`

**لا تلمسه أبداً** (لو احتجت تغييره، اطلب مني):
- أي ملف اسمه ينتهي بـ`.11ty.js` (هذي ملفات تبني الموقع نفسه، خطأ فيها يكسر كل شي)
- مجلد `cf-worker/` (البنية التحتية للإشعارات والإعجابات)
- `.github/workflows/` (آلية النشر التلقائي نفسها)
- `package.json` و`package-lock.json`
- أي شي بملف `style.css` غير الألوان اللي بـ`theme.json` — التصميم (الأحجام، المسافات، الحركات) يحتاج فهم أعمق

---

## لو خربت شي

ما فيه خطر حقيقي — كل تعديل يصير "commit" منفصل، والنسخة القديمة ما تضيع أبداً:
1. روح لصفحة الملف بـGitHub، اضغط **"History"** (تاريخ التعديلات) — تلقى تعديلك بالقائمة بعنوان الرسالة اللي كتبتها.
2. اضغط على التعديل اللي **قبل** تعديلك تشوف محتوى الملف كما كان، وتقدر تنسخه وتلصقه مكان تعديلك الحالي.

**الأسهل من كذا كله: قولي "خربت كذا، رجّعه" وأرجّعه أنا مباشرة عبر git** — دايماً الخيار الأضمن والأسرع.
