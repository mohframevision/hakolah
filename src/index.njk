---
layout: base.njk
permalink: "index.html"
title: "هكوله | كل شيء مفيد في مكان واحد"
description: "هكوله: روابط وأدوات مفيدة، حسابات واشتراكات، مطاعم، متاجر، وصناع محتوى — كل ذلك في موقع واحد."
navActive: "home"
---
<section class="hero">
  <h1>كل شيء مفيد… في موقع واحد</h1>
  <p>روابط وأدوات، حسابات واشتراكات، مطاعم، متاجر، وصناع محتوى — مُجمَّعة ومنظّمة لتوفّر وقتك.</p>
</section>

<section class="section-grid">
  {% for entry in sections %}
  <a class="section-card" href="{{ ('/' + entry.slug + '.html') | url }}">
    <span class="icon">{{ entry.icon }}</span>
    <h3>{{ entry.title }}</h3>
    <p>{{ entry.description }}</p>
  </a>
  {% endfor %}
  <a class="section-card" href="{{ '/favorites.html' | url }}">
    <span class="icon">❤️</span>
    <h3>المفضلة</h3>
    <p>كل ما حفظته من الأقسام المختلفة في مكان واحد.</p>
  </a>
</section>

<div class="ad-slot">مساحة إعلانية (Google AdSense) — ستُفعَّل لاحقاً</div>
