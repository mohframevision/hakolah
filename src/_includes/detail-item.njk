---
layout: base.njk
---
<div class="breadcrumb"><a href="{{ '/index.html' | url }}">الرئيسية</a> / <a href="{{ ('/' + sectionSlug + '.html') | url }}">{{ sectionTitle }}</a> / {{ title }}</div>
<section class="page-header">
  <h1>{{ icon }} {{ title }}</h1>
</section>
{{ content | safe }}
