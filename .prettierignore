# مبني تلقائياً، ما ننسّقه يدوياً
_site
node_modules
package-lock.json

# إعدادات أداة Claude Code نفسها، مو جزء من كود الموقع
.claude

# محتوى Markdown يُدار عبر لوحة تحكم Sveltia CMS — تنسيق Prettier له قد يغيّر
# نص عربي حر (التفاف الأسطر...) بشكل غير مرغوب
src/**/*.md

# قوالب Nunjucks (Prettier ما يدعم .njk بشكل صحيح، وقد يكسر صياغة القوالب)
src/**/*.njk

# ملفات بيانات تُنشأ/تُستبدل تلقائياً عند كل بناء
src/**/*.json
