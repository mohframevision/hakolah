<?xml version="1.0" encoding="utf-8"?>
<!-- المساحات والقياسات هنا منسوخة بالضبط من .item-card بموقع هكوله (style.css):
     padding 20px، فجوة 8px بين العناصر، عنوان 1.05rem، وصف 0.9rem،
     أيقونة 1.8rem، radius 18dp (card_bg.xml) — مو أرقام مخمّنة. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/card_bg"
    android:elevation="3dp"
    android:padding="20dp"
    android:layout_marginStart="16dp"
    android:layout_marginEnd="16dp"
    android:layout_marginBottom="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="top">

        <TextView
            android:id="@+id/itemIcon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="28sp"
            android:layout_marginStart="8dp" />

        <TextView
            android:id="@+id/itemTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textSize="17sp"
            android:textStyle="bold"
            android:textColor="@color/text"
            android:gravity="center_vertical" />

        <TextView
            android:id="@+id/shareButton"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:text="📤"
            android:textSize="16sp"
            android:gravity="center"
            android:clickable="true"
            android:focusable="true"
            android:background="?android:attr/selectableItemBackgroundBorderless" />

        <TextView
            android:id="@+id/favButton"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:textSize="20sp"
            android:gravity="center"
            android:clickable="true"
            android:focusable="true"
            android:background="?android:attr/selectableItemBackgroundBorderless" />
    </LinearLayout>

    <!-- نفس فكرة .item-desc.clamped بالموقع (تقصير + نقاط) — بدون زر "اقرأ
         المزيد" بهذي النسخة، عشان الكرت يضل بارتفاع متناسق بالقائمة -->
    <TextView
        android:id="@+id/itemDesc"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="14sp"
        android:lineSpacingMultiplier="1.3"
        android:textColor="@color/text_muted"
        android:layout_marginTop="8dp"
        android:maxLines="3"
        android:ellipsize="end" />

    <TextView
        android:id="@+id/itemHours"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="12sp"
        android:textColor="@color/text_muted"
        android:layout_marginTop="8dp"
        android:visibility="gone" />

    <!-- ponytail: خانات التصنيف مدمجة بسطر نص واحد بدل رقاقات (chips) منفصلة
         زي الموقع — يحتاج flexbox للف تلقائي وغير متوفر بدون Gradle. أضفها
         لو صار عدد التصنيفات لكل عنصر يستاهل التمييز البصري الفعلي. -->
    <TextView
        android:id="@+id/itemTags"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="12sp"
        android:textStyle="bold"
        android:textColor="@color/text_muted"
        android:layout_marginTop="8dp"
        android:visibility="gone" />

    <HorizontalScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:scrollbars="none"
        android:clipToPadding="false">

        <LinearLayout
            android:id="@+id/itemActions"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal" />
    </HorizontalScrollView>

</LinearLayout>
